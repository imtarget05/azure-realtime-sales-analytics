##############################################################################
# CD — Deploy Azure Functions (Validation Layer)
#
# Triggers: push to main (azure_functions/** changed) or manual
# Fix: functions-action@v2 does not exist → use v1
# Fix: Azure login requires all 3 OIDC params
##############################################################################

name: "CD: Azure Functions"

on:
  push:
    branches: [main]
    paths:
      - "azure_functions/**"
  workflow_dispatch:

env:
  AZURE_FUNCTIONAPP_NAME: func-sales-validation-d9bt2m
  AZURE_FUNCTIONAPP_PACKAGE_PATH: azure_functions
  PYTHON_VERSION: "3.10"

permissions:
  id-token: write
  contents: read

jobs:
  preflight:
    name: "Preflight: Check Secrets"
    runs-on: ubuntu-latest
    outputs:
      azure_ready: ${{ steps.check.outputs.azure_ready }}
    steps:
      - id: check
        env:
          HAS_CLIENT: ${{ secrets.AZURE_CLIENT_ID }}
          HAS_TENANT: ${{ secrets.AZURE_TENANT_ID }}
          HAS_SUB: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
        run: |
          missing=0
          for name in HAS_CLIENT HAS_TENANT HAS_SUB; do
            if [ -z "${!name}" ]; then
              echo "::warning::Missing Azure secret (mapped to $name) — skipping deploy"
              missing=1
            fi
          done
          echo "azure_ready=$([ $missing -eq 0 ] && echo true || echo false)" >> "$GITHUB_OUTPUT"

  deploy:
    name: Build & Deploy Functions
    runs-on: ubuntu-latest
    needs: preflight
    if: needs.preflight.outputs.azure_ready == 'true'
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install system dependencies
        run: |
          sudo apt-get update -q
          sudo apt-get install -y unixodbc-dev

      - name: Prepare package with requirements
        working-directory: ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt --target=".python_packages/lib/site-packages"

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Verify Function App exists
        id: verify
        run: |
          if az functionapp show --name ${{ env.AZURE_FUNCTIONAPP_NAME }} \
             --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
             --query name -o tsv 2>/dev/null; then
            echo "app_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "::warning::Function App '${{ env.AZURE_FUNCTIONAPP_NAME }}' not found or inaccessible — skipping deploy"
            echo "app_exists=false" >> "$GITHUB_OUTPUT"
          fi

      # ── Clean up corrupted state from previous failed deploys ──
      - name: Stop Function App and clear stale settings
        if: steps.verify.outputs.app_exists == 'true'
        run: |
          echo "── Stopping function app to clear malformed state ──"
          az functionapp stop \
            --name ${{ env.AZURE_FUNCTIONAPP_NAME }} \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }}

          echo "── Removing WEBSITE_RUN_FROM_PACKAGE if present ──"
          az functionapp config appsettings delete \
            --name ${{ env.AZURE_FUNCTIONAPP_NAME }} \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
            --setting-names WEBSITE_RUN_FROM_PACKAGE \
            --output none 2>/dev/null || true

          echo "── Setting required app settings ──"
          az functionapp config appsettings set \
            --name ${{ env.AZURE_FUNCTIONAPP_NAME }} \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
            --settings \
              "EVENT_HUB_NAME=sales-events" \
              "SCM_DO_BUILD_DURING_DEPLOYMENT=false" \
              "ENABLE_ORYX_BUILD=false" \
              "AzureWebJobsFeatureFlags=EnableWorkerIndexing" \
            --output none

          echo "── Starting function app ──"
          az functionapp start \
            --name ${{ env.AZURE_FUNCTIONAPP_NAME }} \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }}
          sleep 30

      # ── Create zip and deploy (same method as successful run #12) ──
      - name: Create deployment package
        if: steps.verify.outputs.app_exists == 'true'
        run: |
          cd ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}
          zip -r ../deploy.zip . -x "*.pyc" "__pycache__/*" ".python_packages/lib/site-packages/*.dist-info/*"
          echo "── Zip size: $(du -h ../deploy.zip | cut -f1) ──"

      - name: Deploy via zip push
        if: steps.verify.outputs.app_exists == 'true'
        run: |
          az functionapp deployment source config-zip \
            --name ${{ env.AZURE_FUNCTIONAPP_NAME }} \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
            --src deploy.zip

      - name: Verify deployment
        if: steps.verify.outputs.app_exists == 'true'
        run: |
          sleep 15
          STATUS=$(az functionapp show \
            --name ${{ env.AZURE_FUNCTIONAPP_NAME }} \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
            --query state -o tsv)
          echo "Function App state: $STATUS"
          if [ "$STATUS" != "Running" ]; then
            echo "::warning::Function App is not running (state=$STATUS), attempting restart..."
            az functionapp restart \
              --name ${{ env.AZURE_FUNCTIONAPP_NAME }} \
              --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }}
          fi
