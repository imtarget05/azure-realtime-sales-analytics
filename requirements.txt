# ============================================================
# Root Requirements — Single Source of Truth for ALL dependencies
# Used by: Dockerfile, CI/CD, local development, scripts
#
# Python 3.10+
# ============================================================

# ── Core Framework & Web ──────────────────────────────────
Flask>=3.0.0
gunicorn>=21.2.0
requests>=2.31.0

# ── Data Processing & ML ──────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
matplotlib>=3.7.0
scipy>=1.11.0

# ── ML & Experiment Tracking ──────────────────────────────
mlflow>=2.0.0
# azureml-defaults>=1.50  # Optional: include only in Azure-hosted environments
# azureml-mlflow>=1.50    # Optional: AML MLflow integration

# ── Database & SQL ────────────────────────────────────────
pyodbc>=5.0.0
sqlalchemy>=2.0.0  # For ORM if needed

# ── Azure Services ────────────────────────────────────────
azure-eventhub>=5.15.0
azure-storage-blob>=12.19.0
azure-identity>=1.14.0
azure-keyvault-secrets>=4.7.0
azure-functions>=1.13.0
opencensus-ext-azure>=1.1.0,<2.0

# ── Configuration & Environment ───────────────────────────
python-dotenv>=1.0.0

# ── CLI & Utilities ───────────────────────────────────────
click>=8.1.0
pyyaml>=6.0.0
tqdm>=4.66.0

# ── Testing & Quality ─────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
flake8>=6.1.0
black>=23.12.0
isort>=5.13.0

# ── Monitoring & Logging ──────────────────────────────────
python-json-logger>=2.0.0

# ── Optional: Advanced Features ───────────────────────────
# Uncomment based on your needs:
# tensorflow>=2.13.0       # For neural network models
# torch>=2.0.0             # For PyTorch models
# xgboost>=2.0.0           # For XGBoost models
# lightgbm>=4.0.0          # For LightGBM models
# prophet>=1.1.5           # For time series forecasting
# statsmodels>=0.14.0      # For statistical models
